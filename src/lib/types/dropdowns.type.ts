
import type { Languages } from "$lib/types/language";

export type DropDownLink = {
  label: string;
  link: string;
  text: Languages;
};

export type DropDownItem = {
  label: string;
  text: Languages;
};
